// function anatomy.
// function name is called function anotomy
// for example
// when we define a function we have to put a keyword "function" 
// for example
function fun() {
    console.log("Bennison devadoss")
}
fun();

// Anonymous function
// define a function without name, this function is called anonymous function. 
// we don't call the function without name, we have to declare this function to the variable then we can call the function. 
let fun1 = function () {
    console.log("Bennison Devadoss")
}
fun1();


// Assigining function to a variable. 

let function1 = function (a, b) {
    console.log(a * b);
}
function1(5, 5);

// We can assign the anonymous function to the variable. 
// we can't assigin the named function to the variable.

